# Optional legacy key. The app now prefers "Basemaps → API key basemaps"
# (bring-your-own-key, stored in the browser) — no .env needed.
# VITE_STADIA_MAPS_API_KEY = Enter stadia api key -> https://stadiamaps.com
